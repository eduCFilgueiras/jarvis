[project]
name = "jarvis"
version = "0.1.0"
description = "Modular personal assistant prototype"
requires-python = ">=3.11"

[project.optional-dependencies]
openai = ["openai>=2.0.0"]

[tool.unittest]
start-directory = "tests"
pattern = "test_*.py"
